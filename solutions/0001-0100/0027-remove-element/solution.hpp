#pragma once

#include <vector>

namespace leetcode::p0027 {

// optimized: O(n) time, O(1) extra space — overwrite in place, one pass.
int solve(std::vector<int>& nums, int val);

} // namespace leetcode::p0027
