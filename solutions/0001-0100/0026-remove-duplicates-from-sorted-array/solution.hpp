#pragma once

#include <vector>

namespace leetcode::p0026 {

// optimized: O(n) time, O(1) extra space — two pointers, overwrite in place.
int solve(std::vector<int>& nums);

} // namespace leetcode::p0026
