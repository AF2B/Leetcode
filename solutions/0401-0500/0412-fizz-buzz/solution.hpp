#pragma once

#include <string>
#include <vector>

namespace leetcode::p0412 {

// optimized: O(n) time, O(n) space (excluding the output) — one pass.
std::vector<std::string> solve(int n);

} // namespace leetcode::p0412
